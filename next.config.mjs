/** @type {import('next').NextConfig} */
// const nextConfig = {
//   images: {
//     domains: ["127.0.0.1", "localhost"],

//   },
// };

// export default nextConfig;
const nextConfig = {
  images: {
    domains: [
      "127.0.0.1",
      "localhost",
      "portfolio-api-production-709b.up.railway.app",
    ],
  },
};

export default nextConfig;
